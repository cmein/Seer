All categories list:<br />

<% if !@categories.blank? %>
	<% for item in @categories %>
		<%= link_to item.name, item %> - <%= item.iteration %> - <%= link_to "Feeds", :controller => :feeds, :action => :new, :id => item.id %>  <br />
	<% end %>
<% else %>
	No categories
<% end %>

<%= link_to "New Category", new_category_path %>
